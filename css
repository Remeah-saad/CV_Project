/* Main container styling */
.container {
  width: 80%;
  margin: 0 auto;
  background-color: white;
  padding: 20px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

/* Head styling */
h1 {
  font-size: 2em;
  margin: 0;
}

h2 {
  font-size: 1.5em;
  color: #333;
}

/* Paragraph styling */
p {
  font-size: 1em;
  color: #555;
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
}

.name-title h1 {
  font-size: 2em;
  margin: 0;
}

.name-title p {
  font-size: 1.2em;
  color: #666;
}

.icon {
  width: 65px;
  height: 65px;
}

.icon img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* Contact & Summary styling */
.contact-summary,
.skills-education,
.courses-hobbies {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
}

.contact-summary .left,
.contact-summary .right,
.skills-education .left,
.skills-education .right,
.courses-hobbies .left,
.courses-hobbies .right {
  width: 45%;
}

.vertical-line {
  width: 2px;
  background-color: #ccc;
  height: auto;
  margin: 0 20px;
}

ul {
  padding-left: 0;
  display: flex;
  flex-wrap: wrap;
}

li {
  margin-right: 20px; /* space between elements */
  font-size: 1em;
  color: #555;
}

hr {
  border: none;
  border-top: 2px solid #ccc;
  margin: 20px 0;
}

/* Clearfix */
.clear {
  clear: both;
}
